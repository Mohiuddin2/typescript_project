const person = {
    name: 'Dev7',
    age: 35,
    hobbies: ['chess', 'fchootball'], 
    role: [2, "author"]
}

person.role.push("admin")
console.log(person.role)
// person.hobbies.map(a => console.log(a))
